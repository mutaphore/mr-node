syntax = "proto3";

package workerrpc;

// Worker rpc service definition:w
service Worker {
  // Sends a ping
  rpc Ping (PingRequest) returns (PingReply) {}
}

// The request message
message PingRequest {
  string host = 1;
}

// The response message
message PingReply {
  string host = 1;
}